header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                i.fab.fa-youtube ♥RiaTube♥
        .header__column
            form(action=routes.search, method="get")
                input(type="text", placeholder="検索内容を入力してください。", name="term")
        .header__column
            ul
                if !loggedUser
                    li
                        a(href=routes.join) Join
                    li
                        a(href=routes.login) Log In
                else 
                    li
                        a(href=`/videos${routes.upload}`) Upload
                    li
                        a(href=routes.me) Profile
                    li
                        a(href=routes.logout) Log Out